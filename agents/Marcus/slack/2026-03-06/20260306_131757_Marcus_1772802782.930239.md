# Agent Output

- project: `Main Product`
- agent: `Marcus`
- channel: `C0AJ0740LNB`
- thread_ts: `1772802782.930239`
- message_ts: `1772803077.332019`
- source: `slack`

---

Situation: ✅ Signed-URL batch upload API spec completed — output/specs/upload_signed_url_batch.md

Complication: Maya's UploadStatus UI needs the exact API shape (per-file file_id, expires_at, retry limits, batch id, rate-limit headers) so the UI can show per-file status, expiry countdown and enforce concurrency/retry caps.

Resolution / Next steps:
- Key decisions (summary): max_batch_size=20, max_file_size=25MB, presigned_url_expiry=300s, frontend_concurrency=6, retries=3 (exp backoff), rate_limit=20 req/min. → output/specs/upload_signed_url_batch.md
- #ai-frontend (Kevin) — Use upload_batch_id + per-file file_id. Show statuses: pending / uploading / uploaded / failed, retries_left, expires_at countdown, and “Retry All” that obeys retry cap. Chunk >20 files, concurrency=6, respect X-RateLimit-* and Retry-After.
- Maya — Please either (A) invite maya@team to the Figma file OR (B) export PNG @2x, SVG icons, tokens.json into branch feature/visual-inspection-ui/assets so I can validate file sizes and run integration tests against the upload flow.
- If you need server changes (larger batch size or longer expiry), send use-case + avg file sizes and I’ll adjust (or propose v2 with multipart/resumable uploads).

Created: output/specs/upload_signed_url_batch.md — ping me when assets/figma are ready and I’ll coordinate backend tweaks or tests.